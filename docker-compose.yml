services:
  swap-station:
    build:
      context: .
      dockerfile: Dockerfile
    image: swap-station:latest
    container_name: swap-station
    ports:
      - "3020:3020"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3020
      HOSTNAME: 0.0.0.0